//This page will be rendered if user attend no class
const NoClassWarning:React.FC = ()=>{
    return(
        <div>
            This  is a no class page will be rendered if user have no class
        </div>
    )
};
export default NoClassWarning;